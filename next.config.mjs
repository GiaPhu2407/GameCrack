/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'img.daisyui.com'], // Add the Unsplash domain here
      }, 
};

export default nextConfig;
